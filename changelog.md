# Diyar DVR phase25 v5.34

Что исправлено:
* Причиной неудаления была несовместимая команда find -empty, которая могла не работать на ГУ.
* Очистка теперь работает без неё через безопасный rmdir.
* Поддержаны:records/mosaic/дата;
  records/separate/камера/дата;
  старый прямой формат records/дата.

* Во время записи старые пустые папки проверяются каждые 30 секунд.
* Активная папка сегодняшней записи во время записи не удаляется.
* После нажатия «СТОП» пустые папки очищаются сразу.
* Папка физически не может быть удалена, если внутри есть MP4, скрытый или временный файл.
* Ускорение запуска записи:
* Движок записи теперь запускается до фонового сканирования архива.
* Повторная тестовая запись на носитель убрана из нормального пути «СТАРТ».
* Подсчёт файлов архива не запускается во время записи.
* Очистка служебных папок и проверка незавершённых файлов перенесены в фоновую подготовку.
* Реакция подготовленного движка сокращена с 0,5 до 0,2 секунды.
* Сохранены обязательные проверки лицензии, подключения памяти, минимального свободного места и потери флешки.
* Если фоновая подготовка ещё не готова, остаётся полная безопасная проверка.
